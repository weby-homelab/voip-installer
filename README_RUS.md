# 🛡️ VoIP Server Installer

**Безопасное и автоматизированное развертывание Asterisk на Ubuntu 24.04**

> Готовый установщик защищенного VoIP-сервера: **Asterisk 22** (Docker) + **PJSIP** + шифрование **TLS/SRTP** + **Let's Encrypt** SSL + **Fail2Ban** и **NFTables**. Создан для приватности и простоты использования.

---

# 📞 Инструкция по развертыванию Asterisk

**Версия:** `v4.7.2` (Safe Docker Mode)

Эта инструкция описывает процесс установки безопасной версии сервера VoIP (**Asterisk 22** + **PJSIP** + **TLS/SRTP** + **Fail2Ban** + **NFTables**) на чистый сервер **Ubuntu 24.04**.

### 🌟 Особенности версии

* **Safe Mode:** Не сбрасывает сетевые настройки Docker (no flush ruleset).
* **Безопасность:** Использует TLS 1.3 на порту `5061`.
* **Автоматизация:** Автоматическая генерация SSL через Let's Encrypt.

---

## 🛠 Шаг 1. Подготовка сервера

1. **Зайдите на сервер** по SSH под root:
```bash
ssh root@your-server-ip

```


2. **Проверьте порты:** Убедитесь, что порты `80`, `443` и `5061` свободны.
> *Примечание: Если это чистая установка системы, порты должны быть свободны по умолчанию.*



---

## 📝 Шаг 2. Создание скрипта

1. Создайте пустой файл для скрипта:
```bash
nano install_voip.sh

```


2. **Скопируйте** ПОЛНЫЙ код скрипта `v4.7.2` (`install.sh`) в буфер обмена.
3. **Вставьте** код в терминал:
* **Windows** (PuTTY/PowerShell): Нажмите **ПРАВУЮ** кнопку мыши.
* **Mac/Linux:** `Cmd+V` или `Ctrl+Shift+V`.


4. **Сохраните и закройте файл**:
* Нажмите `Ctrl+O`, затем `Enter`.
* Нажмите `Ctrl+X`.



---

## 🚀 Шаг 3. Запуск установки

1. Дайте права на выполнение:
```bash
chmod +x install_voip.sh

```


2. **Запустите скрипт** (замените данные на свои):
```bash
./install_voip.sh --domain your-domain.com --email admin@your-domain.com

```

**Параметры запуска:**
| Параметр | Описание |
| :--- | :--- |
| `--domain` | Ваше доменное имя (обязательно, должно быть направлено на IP сервера). |
| `--email` | Почта для регистрации сертификата Let's Encrypt. |
| `--ext-ip` | *(Опционально)* Внешний IP, если сервер за NAT. Обычно определяется сам. |

---

## ⚙️ Шаг 4. Что произойдет автоматически

Скрипт выполнит следующие действия без вашего участия:

1. 🐳 Установит **Docker**, **Fail2Ban**, **NFTables**.
2. 🔒 Получит **SSL сертификат** через Certbot.
3. 👤 Сгенерирует пароли для пользователей **100-105**.
4. 🛡️ Настроит firewall (таблица `inet voip_firewall`), **не ломая Docker**.
5. ▶️ Запустит **Asterisk** в контейнере.

---

## ✅ Шаг 5. После установки и проверки

### 1. Получение паролей SIP

Посмотрите сгенерированные пароли:

```bash
cat /root/voip-server/users.env

```


### 2. Статус контейнера

Убедитесь, что контейнер запущен и здоров:

```bash
docker ps

```

> **Ожидается:** Статус `"Up (healthy)"`.

### 3. Проверка Firewall

Проверьте применение правил:

```bash
ft list table inet voip_firewall

```

### 🚨 3.1. Критическая проверка (Docker Network Safety)

Выполните эти команды, чтобы убедиться, что firewall не заблокировал сеть контейнерам:

```bash
systemctl restart nftables
docker exec asterisk-voip curl -Is https://google.com | grep HTTP

```

* **Ожидаемый результат:** `HTTP/2 200` (или `HTTP/1.1 200`).
* **Почему это работает:**
* *Host network:* Контейнер использует стек/IP хоста.
* *Used accept:* Исходящий curl -> SYN -> совпадение 'established' при возврате.
* *No block outbound:* Политика по умолчанию accept (Safe Mode).



> **Успех:** Если тест проходит — Safe Mode полностью защищает сетевую связность Docker.

### 4. Подключение телефона (например, Linphone)

Настройте софтфон, используя следующие данные:

* **Username:** `100` (или 101-105)
* **Password:** *(из файла users.env)*
* **Domain:** `your-domain.com:5061`
* **Transport:** `TLS`
* **Media Encryption:** `SRTP`
* **AVPF:** Disabled (обычно)
* **ICE:** Enabled

---

## 🏗 Архитектура и выбор образа

В проекте используется **`andrius/asterisk:22`** (на базе Alpine Linux) вместо тяжеловесных сборок FreePBX. Это осознанный выбор в пользу **безопасности** и **производительности**.

---
```mermaid
graph TD
    User[SIP Клиент / Софтфон]
    Net[Интернет / WAN]
    FW[NFTables Firewall]
    F2B[Fail2Ban]
    
    subgraph Server [Ubuntu 24.04 Host]
        FW -->|Разрешить TLS 5061| Docker
        FW -->|Разрешить RTP 10000-20000| Docker
        FW -->|Блокировать атаки| Drop[DROP]
        
        F2B -.->|Читает логи| Logs
        F2B -.->|Обновляет бан-лист| FW
        
        subgraph Docker Container [Asterisk 22]
            PJSIP[PJSIP Stack]
            RTP[RTP Engine]
            Logs[Asterisk Logs]
        end
        
        Docker ---|Режим Host Network| FW
    end
    
    User -->|TLS Сигнализация| Net
    User -->|SRTP Голос (Шифрован)| Net
    Net --> FW
```
---
### Почему не FreePBX?
Официальные дистрибутивы (FreePBX) предназначены для управления через GUI, тянут за собой Apache, MySQL, PHP и NodeJS, занимая **>1 ГБ**. Это создает огромную поверхность для атак и усложняет автоматизацию.

### Преимущества текущего решения:
* 🚀 **Легковесность:** Образ весит всего **~60 МБ** (против 1 ГБ+ у аналогов).
* ⚡ **Alpine Linux & Musl:** Минимальное потребление RAM и мгновенный старт контейнера.
* 🛡️ **Безопасность (Hardened):** В контейнере **нет веб-интерфейса**, базы данных и лишних сервисов. Взломать Asterisk через веб-уязвимость невозможно, так как веба просто нет.
* 📄 **Infrastructure as Code (IaC):** Конфигурация генерируется скриптом напрямую в `.conf` файлы. Это надежнее, чем попытки править настройки в базе данных FreePBX через SQL-инъекции.
* 🔧 **Чистый PJSIP:** Используется современный стек SIP без легаси-кода `chan_sip`.

> **Итог:** Это решение для инженеров, которым нужен надежный, быстрый и предсказуемый VoIP-сервер, работающий по принципу "поставил и забыл".

---

## 🔧 Устранение неполадок (Troubleshooting)

* **Нет звука:** Проверьте диапазон UDP `10000-19999` в панели вашего хостинга (например, Hetzner Firewall / AWS Security Group).
* **Ошибка SSL:** Убедитесь, что домен пингуется с самого сервера.
* **Логи Asterisk:**
```bash
docker logs -f asterisk-voip

```

Установку через GitHub (`git clone`) — это гораздо профессиональнее и надежнее, чем копирование текста вручную. Это исключает ошибки кодировки и гарантирует, что вы используете последнюю версию кода.

Инструкция для развертывания прямо из репозитория:

---

# 🐙 Развертывание Asterisk из GitHub

**Репозиторий:** `weby-homelab/voip-installer`
**Цель:** Быстрый старт на чистом сервере Ubuntu 24.04

### 📋 Требования

* Чистый сервер **Ubuntu 24.04**.
* Права **root**.
* Домен, направленный на IP сервера.

---

## 🚀 Вариант 1: Быстрая установка (рекомендуется)

Используйте этот метод для стандартного развертывания. Мы установим git, скачаем репозиторий и сразу запустим скрипт.

### Шаг 1. Подготовка и клонирование

Выполните команды в консоли сервера:

```bash
# 1. Обновляем списки пакетов и ставим git
apt update && apt install -y git

# 2. Клонируем репозиторий
git clone https://github.com/weby-homelab/voip-installer.git

# 3. Переходим в папку
cd voip-installer

```


### Шаг 2. Запуск установки





Даем права на выполнение и запускаем скрипт с вашими параметрами:





```bash


# 1. Делаем скрипт исполняемым


chmod +x install.sh





# 2. Запускаем (ЗАМЕНИТЕ email и domain НА СВОИ!)


./install.sh --email admin@your-domain.com --domain your-domain.com


```





---





## ⚡ Вариант 2: "One-Liner" (для профи)





Если вы хотите выполнить всё одной строкой (удобно для массового развертывания или скриптов cloud-init):





```bash


apt update && apt install -y git && \


git clone https://github.com/weby-homelab/voip-installer.git && \


cd voip-installer && \


chmod +x install.sh && \


./install.sh --email admin@your-domain.com --domain your-domain.com


```

---

## ✅ Что проверять после установки

После того как скрипт отработает, файлы конфигурации могут находиться либо внутри папки репозитория, либо в системных путях (в зависимости от логики скрипта).

1. **Проверка статуса:**
```bash
docker ps

```


*(Должен быть статус `Up (healthy)`)*
2. **Поиск паролей SIP:**
Обычно скрипт генерирует файл `users.env`. Проверьте его наличие:
```bash
# Вариант А (в текущей папке)
cat users.env

# Вариант Б (в папке /root/voip-server, если скрипт создает её)
cat /root/voip-server/users.env

```


3. **Тест безопасности сети (Safe Mode):**
```bash
docker exec asterisk-voip curl -Is https://google.com | grep HTTP

```



---


## 💡 Совет по обновлению

Если вы обновите код в репозитории (например, исправите баги или добавите фичи), на сервере достаточно выполнить:

```bash
cd ~/voip-installer
git pull
# Затем перезапустить скрипт, если требуется обновление конфигурации

```

